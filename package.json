{
  "name": "shop-app",
  "repository": "https://github.com/BKMachine/shop-app.git",
  "author": "Dave <dave@bkmachine.net>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "format": "turbo run format",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "ci": "turbo run ci",
    "check-types": "turbo run check-types",
    "build:server": "docker build -t bkmachine/shop-app .",
    "build:status": "docker build -f ./Dockerfile-status -t bkmachine/cnc-status .",
    "build:haas_serial": "docker build -f ./Dockerfile-haas_serial -t bkmachine/haas-serial .",
    "build:all": "pnpm run build:server && pnpm run build:status",
    "publish:server": "pnpm run build:server && docker push bkmachine/shop-app",
    "publish:status": "pnpm run build:status && docker push bkmachine/cnc-status",
    "publish:haas_serial": "pnpm run build:haas_serial && docker push bkmachine/haas-serial",
    "publish:all": "pnpm run publish:server && pnpm run publish:status && pnpm run publish:haas_serial"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.8",
    "npm-check": "^6.0.1",
    "turbo": "^2.6.1"
  },
  "packageManager": "pnpm@10.23.0",
  "engines": {
    "node": ">=22"
  }
}
